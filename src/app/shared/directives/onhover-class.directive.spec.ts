import { OnhoverClassDirective } from './onhover-class.directive';

describe('OnhoverClassDirective', () => {
  it('should create an instance', () => {
    const directive = new OnhoverClassDirective();
    expect(directive).toBeTruthy();
  });
});
